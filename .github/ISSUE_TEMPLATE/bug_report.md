---
name: Bug report
about: Generated output isn't what it should be, or generate crashed
title: ''
labels: bug
---

## Expected Behavior

## Actual Behavior

## Minimal schema.prisma that reproduces it

Trim it down to the smallest model(s) that still show the problem — that's usually the fastest
way to get a fix (and the easiest thing for a PR to turn into a regression test).

```prisma

```

## Generator config used

```prisma
generator prismaClassGenerator {
  provider = "prisma-class-generator"
  # ...
}
```

## Specifications

-   prisma-class-generator version:
-   Prisma version:
-   Database (postgresql / mysql / mongodb / sqlserver / sqlite / cockroachdb):
-   Node version:
